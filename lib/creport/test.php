#!/usr/bin/php
<?php

## title creport test script title

// $argv = array('coffee', 'brown', 'caffeine');

echo "php test script ", join(" ", $argv), "\n";

// loop through each element in the $argv array
// foreach($argv as $value) { echo "$value\n"; }

?>

